language: java
sudo: false # faster builds

after_success:
  - bash <(curl -s https://raw.githubusercontent.com/codecov/codecov-bash/wip/codecov)
  - pip install --user git+git://github.com/codecov/codecov-python.git@wip && codecov
